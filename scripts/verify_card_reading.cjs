const {chromium}=require('C:/Users/auror/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1536,height:1024}});await p.goto('http://localhost:3129');await p.waitForSelector('.root-card');const card=p.locator('.player .root-card');await card.hover();console.log('hover transform',await card.locator('.card-inner').evaluate(e=>getComputedStyle(e).transform));await card.click();console.log('dialog',await p.locator('#detail').evaluate(e=>e.open),await p.locator('#detail-title').innerText());await p.locator('#close-detail').click();console.log('overflow titles',await p.locator('.card').evaluateAll(cards=>cards.filter(c=>{const f=c.querySelector('.front'),t=c.querySelector('.short');return t.getBoundingClientRect().bottom>f.getBoundingClientRect().bottom-8||t.scrollWidth>t.clientWidth+1}).map(c=>c.getAttribute('aria-label'))));console.log('labels',await p.locator('.player .battle .short').allTextContents());await b.close()})();
