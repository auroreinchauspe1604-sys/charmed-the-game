const fs=require('fs');const p='ecrans/modules/charmed.js';let s=fs.readFileSync(p,'utf8');s=s.replace("$('game-title').textContent=state.title;","$('game-title').textContent=state.title;document.title='Charmed — '+state.title;");s=s.replace("container.append(el('p','empty','Aucun message pour le moment.'));return;","container.append(el('p','empty','Aucun message pour le moment.'));if(container.id==='arbitration'){const f=el('div','facts');f.id='facts';f.append(button('Faits établis',()=>readPages('Faits établis',state.facts.join('\\n\\n'))));container.append(f);}return;");fs.writeFileSync(p,s);
